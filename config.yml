customCommands:
  - key: "C"
    context: "files"
    description: "AI commit"
    prompts:
      - type: "input"
        title: "Commit message"
        initialValue: "{{ runCommand \"bash -lc '$HOME/.config/nvim/scripts/git-commit-ai.py'\" }}"
    command: "git commit -a -m '{{ index .PromptResponses 0 }}'"
    output: terminal
